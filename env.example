# Supabase Database Connection
# Get from Supabase: Project → Database → Connection info → Direct connection
POSTGRES_URL=postgresql+psycopg://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=require

# Base SQL query for your data
# Example for a metrics table with timestamp and numeric columns
BASE_SQL=SELECT ts, metric_value FROM public.metrics WHERE ts >= NOW() - INTERVAL '7 days' ORDER BY ts

# Slack Integration (optional)
# Create webhook: https://api.slack.com/messaging/webhooks
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/XXX/YYY/ZZZ

# Note: We use Phi-4 via Modal's llama.cpp instead of OpenAI
# No external API keys needed - everything runs on Modal!
